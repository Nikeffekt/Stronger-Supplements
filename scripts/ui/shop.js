/* ============================================================
   scripts/ui/shop.js
   Shop Screen – Platzhalter

   Status: Noch nicht implementiert.
   Wird ersetzt sobald der Shop-Screen gebaut wird.

   Abhängigkeiten:
   - scripts/ui/toast.js (toast)
============================================================ */

function zeigeShop() {
  toast('🛒 Shop wird in einer späteren Version implementiert.');
}
